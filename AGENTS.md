# RENJANA — SPBE Disaster Management Platform

Go Fiber + Svelte 5 + Inertia.js + SQLite (modernc) + templ.

> 🔴 **JANGAN jalankan `npm run dev:all` atau dev server apapun.** User yang handle dev server secara manual.

> 🔴 **VERIFIKASI SEBELUM ACTION.** Sebelum buat file baru: cek existing files + wiki + AGENTS.md. Jangan tebak.

## Architecture

Single-entry Go app at `cmd/laju-go/main.go`.

```
routes/web.go → app/handlers/ → app/services/ → app/queries/ → SQLite
                                    ↕
                        Cache (in-memory)
```

- **Handlers**: Parse requests, call services, return responses. **Setiap module/feature harus handler file terpisah.**
- **Services**: Business logic, auth flows, external APIs. Boleh panggil `s.querier.*` atau `s.cache.*`.
- **Queries**: `app/queries/` is **generated by sqlc** from `queries/*.sql`. Never edit manually.
- **Cache**: `app/cache/` — In-memory session cache (sync.RWMutex + map).
- **Frontend**: Svelte 5 + Inertia.js. Entry at `frontend/src/main.ts`. Built to `dist/`.

| Task | npm | Make |
|------|-----|------|
| Dev (both) | `npm run dev:all` | — |
| Dev Go only | `npm run dev:go` | — |
| Build | `npm run build:all` | `make build` |
| Test | `go test ./...` | `make test` |
| Generate sqlc | `npm run db:generate` | `make db-generate` |
| Generate templ | `templ generate` | `make templ` |
| Migrate DB | `npm run db:migrate` | `make migrate` |

> **Build order matters**: `vite build` must run before `go build` because the Go binary reads `dist/.vite/manifest.json`.
> **Migrate via `go run`**: `go run github.com/pressly/goose/v3/cmd/goose@latest` (jangan pake binary `goose`).

## Three-Tier Rule (🔴 CRITICAL)

**Handler → Service → Query → DB.** Tidak ada layer yang boleh lompat.

| Layer | Boleh | Tidak Boleh |
|-------|-------|-------------|
| **Handler** | Parse request, panggil **Service**, return response | Panggil Query, akses DB, business logic |
| **Service** | Business logic, panggil `s.querier.*` atau `s.cache.*` | `sql.Open`, `db.Exec`, raw SQL |
| **Cache** | In-memory session cache (sync.RWMutex + map), dipanggil via Service | Akses langsung dari Handler |
| **Queries** | **SATU-SATUNYA** yang execute SQL | — |

⚠️ Pengecualian: File test (`*_test.go`) BOLEH panggil queries langsung.

🔴 **Ini aturan paling penting — jangan pernah dilanggar.**

## Svelte 5 Rules

- ❌ Jangan `$effect` untuk derived state → ganti `$derived()`
- ❌ Jangan `$effect` untuk init state dari props → `$state(value ?? default)`
- ✅ `$effect` hanya untuk side effects: `document.title`, `localStorage`
- ✅ Internal link WAJIB `use:inertia` dari `@inertiajs/svelte`
- 🔴 **fetch() CSRF header**: tiap `fetch()` ke `/app/*` atau `/admin/*` WAJIB `X-XSRF-TOKEN` dari `getCSRFToken()` (`lib/utils/helpers.ts`). Inertia's `router.*` auto-handle ini.
- Form submission pake `router.post()`/`router.put()`, bukan `<form>` biasa
- File upload via `fetch() + FormData`, simpan URL hasil via `router.put()`
- OAuth links (`/auth/google`, `/auth/github`) pake `<a>` biasa tanpa `use:inertia`

## HTTP Conventions

- POST/PUT redirect: `c.Redirect(path, fiber.StatusSeeOther)` (303, bukan 302)
- PUT/PATCH: return JSON untuk `fetch()`, redirect 303 untuk `router.put()`
- 🔴 **Body parsing**: WAJIB `c.BodyParser()` bukan `c.FormValue()`. Inertia kirim JSON.
- `fiber.Map` untuk adhoc response. Typed structs untuk service boundaries.
- CSRF middleware di `/app/*` routes. Axios auto-send `X-XSRF-TOKEN` dari cookie.

## RBAC Roles

Role hierarchy (tertinggi → terendah):

| Role | Akses |
|------|-------|
| **admin** | Full CRUD semua data, manage users |
| **koordinator** | District-level, scoped ke district sendiri |
| **relawan** | Volunteer, hanya bisa lihat/edit profile sendiri |

Scope filtering via middleware `ScopeDistrict` (set `c.Locals("scope_district_id")` untuk koordinator).

## Database

- SQLite via `modernc.org/sqlite` (pure Go, CGO-free) — static binary tanpa toolchain C.
- Migrations run otomatis di startup via Goose.
- 🔴 **Jangan edit migration yang sudah di-deploy.** Buat file migrasi baru.
- 🔴 **Satu file migrasi = satu tabel** (lihat wiki: [Migration Convention](.llm-wiki/wiki/concepts/migration-convention.md)).
- 🔴 **Cek dulu versi terakhir:** `ls migrations/ | sort | tail -1` — nomor baru = nomor itu + 1.
- `sqlc.yaml` mengatur code generation dari `queries/*.sql`.

## Testing

- `go test ./...` — unit/integration (in-memory SQLite, no external dependencies)
- **agent_browser E2E**: inject session langsung via SQL untuk skip login. Lihat [Agent Browser Testing](.llm-wiki/wiki/concepts/agent-browser-testing.md).

## Gotchas

- `.vite-port` stale? `rm .vite-port && restart Vite`
- `go.sum` is gitignored — `go mod tidy` if needed
- `dist/` gitignored kecuali `.gitkeep`
- Air tidak watch `.templ` — regenerate manual (`templ generate`)

## Wiki (Detail Lebih Lanjut)

Detail lebih lanjut ada di `.llm-wiki/wiki/`. Gunakan `wiki_search`, `wiki_recall`, atau langsung baca file di `.llm-wiki/`:

- [Design Standards](.llm-wiki/wiki/concepts/design-standards.md)
- [HTTP Conventions](.llm-wiki/wiki/concepts/http-conventions.md)
- [Svelte Conventions](.llm-wiki/wiki/concepts/svelte-conventions.md)
- [Three-Tier Rule](.llm-wiki/wiki/concepts/three-tier-rule.md)
- [Handler Structure](.llm-wiki/wiki/concepts/handler-structure.md)
- [Migration Convention](.llm-wiki/wiki/concepts/migration-convention.md)
- [Environment Config](.llm-wiki/wiki/concepts/environment-config.md)
- [Agent Browser Testing](.llm-wiki/wiki/concepts/agent-browser-testing.md)
- [Deployment](.llm-wiki/wiki/concepts/deployment.md)
